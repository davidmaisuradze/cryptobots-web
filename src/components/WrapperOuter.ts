import  styled  from "styled-components";
import { COLOR_CONFIG } from "../config";

export const WrapperOuter = styled.div`
  background: ${COLOR_CONFIG.GRAY_WHITE};
  overflow: hidden;
  height: 100vh;
  width: 100%;
`;
